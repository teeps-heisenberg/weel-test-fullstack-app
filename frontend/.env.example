# =============================================================================
# FRONTEND ENVIRONMENT CONFIGURATION
# =============================================================================
#
# Copy this file to .env for local development without Docker
# For Docker setup, values are configured in docker-compose.yml

VITE_API_URL=http://localhost:5000/api
PORT=3000
